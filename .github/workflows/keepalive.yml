name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # каждые 5 минут
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl health
        run: curl -fsS https://mrhouseklg-bot.onrender.com/ || true